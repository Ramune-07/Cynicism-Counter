# Discord Bot Token from Developer Portal
DISCORD_TOKEN=your_token_here
# 特定のユーザーの全発言をカウントするためのロールID（オプション。設定しない場合は空で構いません）
TARGET_ROLE_ID=
# カウントが閾値を超えた際に付与するロールのID（オプション）
REWARD_ROLE_ID=
# ロール付与のカウント閾値（オプション。例: 10）
REWARD_COUNT_THRESHOLD=